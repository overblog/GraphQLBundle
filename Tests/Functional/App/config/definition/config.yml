imports:
    - { resource: ../config.yml }

overblog_graphql:
    definitions:
        schema:
            query: RootQuery
        mappings:
            types:
                -
                    type: yml
                    dir: "%kernel.root_dir%/config/definition/mapping"
