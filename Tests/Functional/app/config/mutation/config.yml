imports:
    - { resource: ../config.yml }
    - { resource: services.yml }

overblog_graphql:
    definitions:
        schema:
            query: RootMutation
            mutation: RootMutation
        mappings:
            types:
                -
                    type: yml
                    dir: %kernel.root_dir%/config/mutation/mapping
