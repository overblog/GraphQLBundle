tools:
    external_code_coverage:
        timeout: 1200
build:
    nodes:
        analysis:
            tests:
                override:
                    - php-scrutinizer-run

filter:
    excluded_paths:
        - "tests/"
