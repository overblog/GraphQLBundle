Photo:
    type: object
    config:
        fields:
            id:
                type: ID!
            width:
                type: Int
        resolveField: "@=query('resolve_photo_field', value, info)"
        interfaces: [Node]
