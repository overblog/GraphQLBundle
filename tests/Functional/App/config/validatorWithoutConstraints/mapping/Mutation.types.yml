Mutation:
    type: object
    config:
        fields:
            noValidationDefined:
                type: Boolean
                resolve: '@=m("mutation_mock", args, validator)'
                args:
                    username:
                        type: "String"
