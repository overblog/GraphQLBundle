$validator = new ArgumentsValidator(
<spaces>func_get_args(),
<spaces>$globalVariable->get('container')->get('validator'),
<spaces>$globalVariable->get('container')->get(ValidatorFactory::class),
<spaces>[
<spaces><spaces>'class' => <classValidation>,
<spaces><spaces>'properties' => <validationMapping>
]
);
